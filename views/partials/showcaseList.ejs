<div class="container">
    <h2><a href="<%= item=='rent'?'/rentals':'/forSale' %>"><%= title %> </a></h2>
    <div class="row">
      <% if(items.length>0){ %>
        <% items.forEach((item, index) => { %>
          <%- include('showcaseItem', { item: item }) -%>
          <% if ((index + 1) % 3 === 0 && index !== items.length - 1) { %>
            </div>
            <div class="row">
          <% } %>
        <% }); %>
   
        <%   }  else {%>
        <p class=" dangarous text-center">😥samahani hakuna nyumba za <%= item=='rent'?' kupanga':'kuuza' %>  kwa sasa 😒</p>
      <% } %>
     
    </div>
  </div>
  
